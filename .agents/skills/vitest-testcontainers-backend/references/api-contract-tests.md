# API Contract Tests

- Assert authentication failures.
- Assert permission-denied and hidden-resource behavior.
- Assert DTO validation failures.
- Assert status codes and error envelopes.
- Assert response DTO shape, not raw database rows.
- Assert pagination bounds and sorting allowlists.
