# N Plus One Review

- Count SQL statements per request.
- Find loops that call repositories or loaders per row.
- Replace per-row reads with joins or batched queries.
- Keep authorization and tenant scope in the batched query.
- Verify the response does not return unnecessary nested children.
