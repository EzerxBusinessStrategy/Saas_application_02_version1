# API contract rules

Use plural, kebab-case resources; stable DTOs; explicit pagination/filter/sort metadata; ISO dates; stable errors; request validation; tenant context; and backward-compatible versioning. Do not change public contracts without approval and an ADR.
