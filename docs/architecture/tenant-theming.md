# Tenant theming

Tenant themes contain a validated name, six-digit primary colour, and optional logo URL. Semantic CSS variables map the active primary colour to the whole interface. Warning and danger colours remain fixed for safety semantics.
