# State management

TanStack Query is the server-data boundary and Zustand is reserved for ephemeral UI preferences when added. Filters, sorting, and pagination should be URL search parameters. Fixture data is never stored in global UI state.
