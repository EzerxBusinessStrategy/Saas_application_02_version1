# Cache rules

Cache only when measured benefit exceeds correctness/security risk. Define source of truth, tenant-namespaced key, TTL, invalidation, permission sensitivity, stale tolerance, failure behavior, and observability. Reject caches that can leak tenant or personal data.
