# E2E scenarios

Cover tenant admin client-to-task flow, employee task update, manager review, finance invoice send, unauthorised route behavior, and Tenant A/B isolation after authentication/API contracts exist. The initial browser test verifies that the public fixture shell does not expose a second tenant.
