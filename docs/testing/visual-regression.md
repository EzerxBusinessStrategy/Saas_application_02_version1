# Visual regression

For each priority Figma frame: run the app, set Figma viewport dimensions, capture route screenshot, compare, log differences in `docs/figma/visual-differences.md`, correct semantic layout differences, and recapture.
