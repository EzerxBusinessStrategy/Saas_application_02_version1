# Design system

Use `Button`, `Card`, `Badge`, `Dialog`, metric cards, status badges, and empty states before adding custom markup. Components use semantic tokens from `src/app/globals.css` and are source-owned in the shadcn-compatible `src/components/ui` directory.
