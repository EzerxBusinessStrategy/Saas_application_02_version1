# Accessibility

The shell includes a skip link, semantic landmarks, visible focus rings, labelled controls, accessible table captions, dialog title, and reduced-motion handling. Preserve these requirements in all additions; colour never carries status alone.
