# Responsive rules

Desktop uses a fixed sidebar; small screens collapse navigation controls. KPI cards stack from four to two to one, board columns scroll horizontally, and tables scroll inside their local container.
