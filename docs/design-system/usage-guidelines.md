# Usage guidelines

Prefer shared components, semantic tokens, short active labels, and empty/error states that tell the user what can happen next. Add feature-specific primitives only when an existing shared primitive cannot express the requirement.
