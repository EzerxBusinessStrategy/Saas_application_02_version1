# Authorisation standard

Enforce backend policies before protected operations and verify membership, tenant, resource, manager, employee, client, and support scope. Default deny and audit sensitive support access. UI filtering is presentation only.
