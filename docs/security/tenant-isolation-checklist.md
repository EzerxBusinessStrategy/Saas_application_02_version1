# Tenant isolation checklist

Check tenant IDs, scoped queries/joins, RLS, cache keys, queue context, storage paths, WebSocket rooms, search, logs, exports, reports, signed URLs, and audited Platform Operator access. Require automated Tenant A/B tests.
