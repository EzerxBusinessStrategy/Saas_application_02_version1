# Security review standard

Review real code paths for auth bypass, horizontal/vertical escalation, tenant leakage, injection, XSS, CSRF, SSRF, uploads, IDOR, secrets, logging, dependencies, sessions, redirects, traversal, commands, races, validation, cache, and queue payloads. Report severity, confidence, evidence, files, scenario, impact, correction, test, and approval boundary.
