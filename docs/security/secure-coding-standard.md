# Secure coding standard

Validate inputs, encode output, use parameterised database access, avoid unsafe redirects/deserialisation/commands, constrain uploads, prevent SSRF/path traversal, handle errors safely, and do not log secrets or sensitive data.
